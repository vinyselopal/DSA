class Edge {
  constructor(from, to, weight) {
    (this.from = from), (this.to = to), (this.weight = weight);
  }
}

class WeightedDirectedGraph {
  constructor(edges) {
    
  }
}

const G = new WeightedDirectedGraph([[]]);
